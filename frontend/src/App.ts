export function renderApp(root: HTMLElement): void {
  root.innerHTML = `<main style="font-family:system-ui, sans-serif; padding:24px;">
    <h1>Legacy Frontend Disabled</h1>
    <p>This minimal shell remains for backwards compatibility. Please use the new UI at <code>/ui</code>.</p>
  </main>`;
}
