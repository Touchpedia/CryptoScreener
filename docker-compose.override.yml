services:
  backend:
    command: >
      sh -c 'uvicorn main:app --host 0.0.0.0 --port 8000'
    environment:
      - REDIS_URL=redis://cs_redis:6379/0
      - RQ_QUEUE=ingestion-tasks

  worker:
    command: >
      sh -c 'rq worker ingestion-tasks --url redis://cs_redis:6379/0'